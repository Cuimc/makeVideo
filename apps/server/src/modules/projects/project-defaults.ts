import type { ProjectCreativeParams } from '@make-video/shared';

export const DEFAULT_PROJECT_FORM: ProjectCreativeParams = {
  videoType: 'news-explainer',
  style: 'clean',
  platform: 'douyin',
  durationSeconds: 60,
  targetAudience: 'general',
};
